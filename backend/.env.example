# Server configuration
PORT=4000
NODE_ENV=development

# OpenAI configuration
OPENAI_API_KEY=replace-with-your-openai-key
OPENAI_MODEL=gpt-4.1-mini

# Chart Analysis configuration
# Temperature for AI model (0.0 = deterministic, 2.0 = creative)
# Recommended: 0.1-0.3 for consistent technical analysis
# CHART_ANALYSIS_TEMPERATURE=0.2

# Finnhub configuration
FINNHUB_API_KEY=replace-with-your-finnhub-key
# FINNHUB_BASE_URL=https://finnhub.io/api/v1

# Database configuration
# Example: postgres://user:password@localhost:5432/equity_insight
DATABASE_URL=

# Debate round configuration for chart debate (Agent A/B)
# These are for the chart analyst image debate (Agent A/B/Referee), separate from TradingAgents.
# Total turns for Agent A (swing trader). Minimum 1. Default 1.
# CHART_DEBATE_A_ROUNDS=1
# Total turns for Agent B (risk manager). Minimum 0. Default 1.
# CHART_DEBATE_B_ROUNDS=1

# Logging directories
# Override default log directories if you want logs outside the repo.
# TA_LOG_DIR=./backend/logs
# CHART_DEBATE_LOG_DIR=./backend/chartlogs
